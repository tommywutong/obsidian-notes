---
title: "【iOS】Runtime 系列 · 总览与展开大纲"
description: "iOS Runtime 系列的主线逻辑、各篇展开目录与后续篇章规划。系列的单一事实来源（SSOT）。"
tags: ["iOS", "Objective-C", "Runtime", "系列大纲"]
category: "iOS"
series: "iOS Runtime 系列"
seriesSlug: "ios-runtime"
seriesOrder: 0
draft: true
---

# 这份文档是什么

「iOS Runtime 系列」的**总目录与展开大纲**，作为整个系列的单一事实来源（SSOT）。
重建于 2026-06-21，依据是 `iOS/20 专题笔记/Runtime/` 下各 Part 的真实章节结构反推而成。

> 工作区分工：
> - **`iOS/20 专题笔记/Runtime/`** = 主工作区（系列正文，配套 `.html` 可视化图统一放在 `素材/`）。
> - **`TommyWu Lab/`** = 博客发布产物（`ios-runtime-part-N-*.md`），从工作区同步过去。

---

# 主线逻辑（为什么这样排）

整条线是「**静态结构 → 动态调用 → 扩展机制 → 工程落地**」一路下沉再上浮：

1. **Part 1 对象与类的本质** —— 先把「东西长什么样」讲清楚：对象、类、元类、`isa`、内存布局。
2. **Part 2 消息发送与转发** —— 再讲「调用怎么跑」：`objc_msgSend` 的快速缓存、慢速查找、动态解析、消息转发。
3. **Part 3 Category** —— 在前两篇的结构之上，讲「运行时怎么往类里加东西」：加载时机、方法附加、覆盖假象、关联对象。
4. **Part 4 应用篇** —— 把底层能力收口到业务：动态方法、Swizzling、KVO、AOP、自动归档。

Part 3 末尾「把 Part 1 和 Part 2 连起来」、Part 4 开头的回顾，都是有意做的承接。

---

# 已写篇章 · 展开目录

## Part 1 · 对象与类的本质  ✅ `draft: false`
- Runtime 简介
- 对象的本质 `objc_object`：骨架 → `isa_t`（含 arm64e PAC 签名）→ Tagged Pointer 优化
- 对象的内存布局：isa + ivar、对象多大（需求/实分）、NSObject 的 8/16
- 类的本质 `objc_class`：类也是对象、四大件（isa / superclass / cache / bits）
- 元类 metaclass：元类也是 objc_class、类方法 = 元类的实例方法
- isa 走位与继承链：realize 接环、isKindOf/isMember、self vs super
- 入院题：Class 与内存地址

## Part 2 · 消息发送与转发  ✅（145KB，最详尽）
- `objc_msgSend` 简介
- 第一部分 · 快速路径（缓存命中，cache_t）
- 第二部分 · 慢速查找 `lookUpImpOrForward`
- 第三部分 · 消息转发：动态方法解析 `resolveInstanceMethod:` → 转发链
- 第四部分 · 实战与收尾（断点观测各站）

## Part 3 · Category：加载、覆盖与关联对象  ✅
- 结论先行：Category 的本质
- 为什么能加方法、不能加 ivar（OC 2.0 拆分 ro / rw）
- 编译产物 `category_t`
- 目标类侧：`class_rw_t` / `class_rw_ext_t`、`method_array_t`、`attachLists` 插到最前
- 「覆盖」假象的真相
- 加载时机与流程总览、新旧源码对照
- Category 与 Class 的 `+load`（顺序、为何不覆盖；对比 `+initialize`）
- 关联对象：三个 API、如何绕开「不能加 ivar」
- 历史旁注（OC 1.0）、例题、Q&A

## Part 4 · Runtime 应用篇  🚧 `draft: true`（进行中）
1. 动态获取类信息
2. 动态添加方法
3. Method Swizzling（本质 → 最小实现 → `+load+dispatch_once+class_addMethod` 模板 → 调原实现 → class cluster 真实类探测）
4. 关联对象应用
5. 消息转发应用（组合对象能力透传 / 模拟多继承）
6. NSProxy + `forwardInvocation:` 实现 AOP
7. Isa Swizzling 与 KVO
8. 基于遍历的自动归档与模型转换（NSCoding、字典↔模型）
9. Runtime 应用边界 + 小结

---

# 候选后续篇章（建议，未开写）

以下是基于现有内容的自然延伸，**仅为规划候选**，确认后再排期：

- **Part 5 · weak 与引用计数**：`isa` 的 `has_sidetable_rc`、SideTable、weak 表、`weak_entry_t`、对象释放清理。（vault 里已有 `RunLoop 与 AutoReleasePool.md` 可联动）
- **Part 6 · 类的加载链路**：`_objc_init`、`map_images` / `load_images`、`+load` 调用全过程、与 dyld 的衔接。（已有 `iOS/20 专题笔记/编译链接与启动/dyld.md`）
- **Part 7 · KVO 原理深挖**：把 Part 4 第 7 节的 isa swizzling 单独展开，讲 `NSKVONotifying_` 派生类、自动/手动通知。

> 维护约定：**新增或调整篇章时，先改这份大纲，再动正文**，保证它始终是 SSOT。
